name: Deprecated split deterministic tier workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  moved-to-tiered-deterministic-workflow:
    name: Use Tiered Deterministic Test Suite
    runs-on: ubuntu-24.04
    steps:
      - name: Explain replacement workflow
        run: |
          echo "This split workflow is retained only to avoid dangling workflow-file references."
          echo "Deterministic CI now runs in .github/workflows/core-tests.yml as the Tiered Deterministic Test Suite."
